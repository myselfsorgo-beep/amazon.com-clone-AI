# Amazon Clone

A simple responsive shopping landing page built with HTML, CSS, and JavaScript.

## Run locally

Open index.html in your browser, or use a simple local server such as:

```bash
python -m http.server 8000
```

Then visit http://localhost:8000.
